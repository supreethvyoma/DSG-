Upload the contents of this package to your Hostinger public web root.

Notes:
- This is the built React frontend.
- The included .htaccess file enables React Router refresh/deep-link support.
- If your frontend and backend are on different domains, rebuild the app with VITE_API_BASE_URL set to your backend URL before deploying.
