Upload this package to your Hostinger Node.js app directory.

Before starting:
- Create backend/.env from backend/.env.example
- Set MONGO_URI, JWT_SECRET, PORT, and any payment variables you use

Start command:
- npm install
- npm start

Startup file:
- server.js
